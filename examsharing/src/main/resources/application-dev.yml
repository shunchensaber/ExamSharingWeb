spring:

  datasource:
    driver-class-name: com.mysql.jdbc.Driver             # 数据库链接
    url: none

  jpa:
    hibernate:                                           # Hibernate管理
      ddl-auto: update
    show-sql: true
  # 开发过程关闭缓存
  thymeleaf:
    cache: false


